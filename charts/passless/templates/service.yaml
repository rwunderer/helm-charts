apiVersion: v1
kind: Service
metadata:
  name: {{ include "passless.fullname" . }}
  labels:
    {{- include "passless.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "passless.selectorLabels" . | nindent 4 }}
