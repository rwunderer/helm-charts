apiVersion: v1
kind: Service
metadata:
  name: {{ include "passless-operator.fullname" . }}
  labels:
    {{- include "passless-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "passless-operator.selectorLabels" . | nindent 4 }}
